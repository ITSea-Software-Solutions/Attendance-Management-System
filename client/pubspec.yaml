name: ams_client
description: "TrueCrew — Every worker verified — Aadhaar + biometric attendance (offline-first)."
publish_to: "none"
version: 0.9.14+15

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  dio: ^5.4.0
  ffi: ^2.1.0
  sqflite_common_ffi: ^2.3.0
  sqlite3_flutter_libs: ^0.5.20
  path_provider: ^2.1.2
  path: ^1.9.0
  uuid: ^4.3.3
  intl: ^0.19.0
  connectivity_plus: ^6.0.1
  image_picker: ^1.1.0
  google_mlkit_face_detection: ^0.13.1
  camera: ^0.11.0+2
  camera_windows: ^0.2.6
  file_picker: ^10.3.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
